{
  "entities": {
    "ChatMessagePart": {
      "title": "ChatMessagePart",
      "description": "A structural part of a chat message containing text details.",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The textual content of the message part"
        }
      },
      "required": ["text"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "A single chat message exchanged between the user and model.",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the message"
        },
        "role": {
          "type": "string",
          "enum": ["user", "model"],
          "description": "The sender of the message"
        },
        "parts": {
          "type": "array",
          "description": "The rich content components of the message"
        },
        "thought": {
          "type": "string",
          "description": "Optional internal deep-thinking / reasoning process"
        }
      },
      "required": ["id", "role", "parts"]
    },
    "Chat": {
      "title": "Chat",
      "description": "A full, structured chat conversation",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique UUID of the chat session"
        },
        "title": {
          "type": "string",
          "description": "The user-friendly title of the conversation"
        },
        "messages": {
          "type": "array",
          "description": "List of ChatMessages representing the conversation history"
        },
        "pinned": {
          "type": "boolean",
          "description": "Whether the chat is pinned in the user's list"
        },
        "userId": {
          "type": "string",
          "description": "The unique user identifier who owns this conversational study"
        },
        "updatedAt": {
          "type": "integer",
          "description": "A server-synchronized epoch timestamp of the last edit"
        }
      },
      "required": ["id", "title", "messages", "pinned", "userId", "updatedAt"]
    }
  },
  "firestore": {
    "chats/{chatId}": {
      "schema": "Chat",
      "description": "The main collection of active user study chats"
    }
  }
}
